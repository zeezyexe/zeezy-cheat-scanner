#pragma once

// Generated by CMake from version.hpp.in - do not edit directly. The
// version string comes from the single project(... VERSION ...) call in
// CMakeLists.txt, so the UI and the build can never silently drift apart.
#define DAXY_VERSION_STRING "v@PROJECT_VERSION@"
